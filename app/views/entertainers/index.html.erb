<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h1>All Entertainers</h1>
      <ul>
        <% @entertainers.each do |entertainer| %>
          <li><%= link_to entertainer.username, entertainer_path(entertainer) %>: <%= entertainer.job_type %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
